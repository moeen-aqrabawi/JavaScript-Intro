
<?php
	// include connectDB.php
	/* variables from connectDB.php
		$dbConn - connection object
	*/
	
	// while to sift through all records
	while
	{
		// if to check if userName already exists
		if (
	
		// else if to check if First and Last name already exists
			elseif(
	} 
	// if returnToLogin == false
		if(

	} // end if isset
	
	// include closeDB.php

?>