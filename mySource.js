function checkRange(x,low,high)
{
	//create range-check function
}
function charAtPlace(x, y, z)
{
	//create function to find a specific character at a specific place
}
function checkForChar(x, y)
{
	//create function to find a specific character
}
function checkPercent(x, y, z)
{
	//create function to calculate a percentage
}
function checkWord(x,y)
{
	//create function to check if one word matches another
}
function buildTable(rows, cols, fill, style)
{	
	//create a function to build a table with user choices for:
	//number rows and columns
	//cell fill
}